//
// pwd.cmm : カレントディレクトリを表示する
//
#include <stdio.hmm>
#include <stdlib.hmm>

public int main() {
  char[] pwd = getEnv("PWD");
  if (pwd == null) pwd = "(null)";
  printf("%s\n", pwd);
  return 0;
}
