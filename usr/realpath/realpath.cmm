#include <stdio.hmm>
#include <stdlib.hmm>
#define  BUF 80
char[] buf = array(BUF);
public int main(int argc, char[][] argv) {
  if (argc < 2) {
    printf("realpath: missing operand\n");
    return 1;
  }
  absPath(argv[1], buf, BUF);
  printf("%s\n", buf);
  return 0;
}
