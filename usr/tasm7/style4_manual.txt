スタイル4   LD | ADD | SUB | CMP | AND | OR | XOR

pc部分 | ラベル部分 | 予約語部分 | ニーモニック
    処理が同じなので省略

機械語部分
    ・op = searchMachineCode(token_id)を実行
    ・getToken()を使ってトークン番号を取り出し、token_idよりgrの値をセット
    ・式の部分を取り出すためにgetToken()を実行
    ・opr1[]にgetString()
    ・setMnemonic(opr1)
    ・opr2[]にgetString()
    ・setMnemonic(opr2)
    ・'#'ならイミディエイト、','ならインデクスドでgetTokenもろもろ、
    　それ以外なら数値と決まるので、この時点でxr部を決定
    ・first_byteにopとgrとxrのANDを取った物を代入
    ・setFirstByte(toHex(first_byte))を実行
    ・machine_code[cnt]にfirst_byteを代入
    ・cntを増加
    ・dataに対してeqAnalyzer(opr2)を実行
    ・dataを16進int型に変更してsecond_byteに代入
    ・setSecondByte(toHex(second_byte))を実行
    ・machine_code[cnt]にsecond_byteを代入
    ・cnt増加
